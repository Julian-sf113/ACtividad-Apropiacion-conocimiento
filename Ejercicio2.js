//Inicializamos variables 
let base = prompt("Ingrese la base del triangulos: "); //Ingresamos la base del triangulo
let altura = prompt("Ingrese la altura del triangulos: "); // Ingresamos la altura del triangulo
 // hacemos la operacion para encontrar el area
let area = (base * altura) / 2; //Operacion para hallar el area
//imprimimos el resultado
alert("El area es: " + area)